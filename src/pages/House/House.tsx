import React, {
    memo,
    //  useEffect, 
    //  useRef 
} from "react";
import {
    Container,
} from "@mui/material";


const House: React.FC = () => {

    return (
        <React.Fragment>
            <Container sx={{ overflow: 'hidden' }}>

            </Container>
        </React.Fragment>
    )
};

export default memo(House);